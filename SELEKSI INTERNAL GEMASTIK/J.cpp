#include <bits/stdc++.h>
using namespace std;

int main () {
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    int N, M;
        cin >> N >> M;
    int arr[N][M];
        for (int i = 0; i < M; i++){
            for (int j = 0; j <= 1; j++)
            {
                cin >> arr[i][j];
                if(arr[i] == arr[j]) {
                    
                }
            }
        }
         
}