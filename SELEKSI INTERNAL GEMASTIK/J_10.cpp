#include <bits/stdc++.h>
using namespace std;

int main(){
    int N,S,P;
        cin >> N >> S >> P;
    int ganas[N], pasien[N];
    string sebar[N];
    
    for (int i = 0; i < N; i++){
        cin >> ganas[i];
    }
    for (int i = 0; i < N; i++){
        cin >> sebar[i] >> pasien[i];
    }
    
    
}